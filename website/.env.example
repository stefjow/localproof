# Copy to .env (gitignored) and fill in.

# Flask session signing key. Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Set to 1 to enable the Flask debugger. Never in production.
FLASK_DEBUG=0
